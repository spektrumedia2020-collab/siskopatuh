import { initializeApp } from "firebase/app";
import { getFirestore, collection, getDocs } from "firebase/firestore";
// Need the config to connect. 
// Or I can just check the browser console for errors!
